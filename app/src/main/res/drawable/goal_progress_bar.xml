<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/goal_progress_bar_track_gradient1"
                android:centerColor="@color/goal_progress_bar_track_gradient2"
                android:centerY="1.0"
                android:endColor="@color/goal_progress_bar_track_gradient3"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/goal_progress_bar_gradient1"
                    android:centerColor="@color/goal_progress_bar_gradient2"
                    android:centerY="1.0"
                    android:endColor="@color/goal_progress_bar_gradient3"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>